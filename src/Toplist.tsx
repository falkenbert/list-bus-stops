import React from "react";

interface Props {
    list: {
        nameLabel: string,
        total: number
    }
}

export const Toplist: React.FC<Props> = () => {
    return (
        <div>

        </div>
    );
}